[Unit]
Description=Taconez Playback Distributor
BindsTo=docker.service
After=docker.service

[Service]
Type=exec
Restart=always
Environment=TACONEZ_VERSION={{ project_version }}
ExecStart={{ services_root_remote }}/playback-distributor/exec-start.sh
ExecStop={{ services_root_remote }}/playback-distributor/exec-stop.sh
TimeoutStartSec=900

[Install]
WantedBy=multi-user.target
